name: Generate SQL Files

on:
  push:
    branches:
      - main

jobs:
  generate_file:
    name: Generate SQL Files
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.12.1' 

    - name: Run Python script
      run: python SQL/concat.py

    - name: Commit changes
      run: |
        git config --local user.email sebastiancaron62@gmail.com"
        git config --local user.name "Sebastian"
        git add .
        git commit -m "Add SQL Auto Generated files"
        git push
